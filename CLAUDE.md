# CLAUDE.md

このファイルは、このリポジトリで作業する Claude Code (claude.ai/code) 向けのガイダンスです。

## プロジェクト概要

Unity 製のシングルプレイヤー・オートバトルローグライクゲーム。

## 仕様ドキュメントについて

`Docs/` ディレクトリに、ゲームの仕様・設計に関するドキュメントが置かれています。
実装や設計判断を行う際は、必ず `Docs/` 配下のマークダウンファイルを参照してください（内容はここには転記しません。最新情報は常に `Docs/` を直接確認すること）。

- `Docs/ゲーム概要.md` — ゲームループ、戦闘、敵の出現、アイテムの選択・装着などゲーム全体の仕様
- `Docs/キャラクター.md` — ロール、ステータス、通常攻撃/アクティブスキル/パッシブスキル、行動(Action)システム、状態変化、タグ、アイテムの仕様
- `Docs/実装したいデバッグ機能.md` — シミュレーション、高速戦闘、1フレーム毎の進行、シード値固定、任意の状態の再現などデバッグ機能の要望

`Docs/` にドキュメントが追加・更新された場合は、そちらの内容を優先して参照してください。

## コードベースに関する注意事項

- `Assets/Scripts_Test/`（`namespace test`）配下のクラスは試作段階で作成したものです。現行実装の参考にする必要はなく、無視して構いません。本実装は `Assets/Scripts/` 配下で行います。

## 実装完了時の報告ルール

実装作業を完了した際は、その作業で **新規作成・変更・削除したファイルの一覧を必ず提示すること**。
- 新規作成 (Added)
- 変更 (Modified)
- 削除 (Deleted)

の3種類に区別して列挙すること。
